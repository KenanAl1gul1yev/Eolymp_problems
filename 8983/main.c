#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum = 0, i = 0, num;
    char operand;
    while (1){
        if (i % 2 == 0) scanf("%d", &num);
        else{

        }
        i += 1;
    }
    return 0;
}
